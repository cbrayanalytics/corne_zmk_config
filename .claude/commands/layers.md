Read config/corne.keymap and display a clean, human-readable summary of every layer.

For each layer, show:
- The layer name and index number
- How to access it (e.g. "hold right-thumb BSPC" for ADJUST)
- The full key grid as a labeled ASCII table (left half | right half), with home-row mod assignments called out

Keep the output compact enough to scan at a glance.
